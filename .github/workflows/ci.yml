# Context Engine CI — M0 bootstrap.
#
# GH-hosted runners ONLY for now: migrating the Windows leg to the org's self-hosted Windows
# runner is a deliberate later follow-up, not part of the M0 bootstrap.
#
# Toolchain note: these jobs use the runner-default toolchains (Clang on Linux, Apple Clang on
# macOS, MSVC on Windows). The L-42 per-target pinned Clang toolchain manifest is an M0
# follow-up and will replace the defaults here.

name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    name: build (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            generator: Ninja
            cc: clang
            cxx: clang++
          - os: macos-latest
            generator: Ninja
            cc: ''
            cxx: ''
          - os: windows-latest # default generator (Visual Studio) — no MSVC env setup needed
            generator: ''
            cc: ''
            cxx: ''
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Configure environment
        shell: bash
        run: |
          if [ -n "${{ matrix.generator }}" ]; then
            echo "CMAKE_GENERATOR=${{ matrix.generator }}" >> "$GITHUB_ENV"
          fi
          if [ -n "${{ matrix.cc }}" ]; then
            echo "CC=${{ matrix.cc }}" >> "$GITHUB_ENV"
            echo "CXX=${{ matrix.cxx }}" >> "$GITHUB_ENV"
          fi

      # Build files live in src/ (root stays minimal): configure with -S src; build/test
      # presets resolve CMakePresets.json from the working directory, so those run in src/.
      - name: Configure
        run: cmake -S src --preset dev

      - name: Build
        run: cmake --build --preset dev
        working-directory: src

      - name: Run context-hello
        run: ctest --preset dev --verbose
        working-directory: src

  sanitize:
    name: sanitize (ASan+UBSan, ubuntu)
    runs-on: ubuntu-latest
    env:
      CMAKE_GENERATOR: Ninja
      CC: clang
      CXX: clang++
    steps:
      - uses: actions/checkout@v4

      - name: Configure (sanitize preset)
        run: cmake -S src --preset sanitize

      - name: Build
        run: cmake --build --preset sanitize
        working-directory: src

      - name: Run context-hello under ASan+UBSan
        run: ctest --preset sanitize --verbose
        working-directory: src

  # R-FILE-011 benchmark baseline on a SMALL corpus (10k files). GH-hosted runners are
  # NOT the R-QA-009 perf-isolated runner class — this job is a trend gauge + harness
  # smoke (results archived as artifacts), not a gating floor. The full 100k corpus run
  # stays local/perf-box. Spike targets are opt-in (CONTEXT_BUILD_SPIKES=ON), so the
  # default build matrix above stays vcpkg-free.
  bench-baseline:
    name: bench baseline (10k corpus, ubuntu)
    runs-on: ubuntu-latest
    env:
      CMAKE_GENERATOR: Ninja
      CC: clang
      CXX: clang++
    steps:
      - uses: actions/checkout@v4

      - name: Generate seeded 10k corpus
        run: python3 bench/gen_corpus.py --size 10000 --out bench/corpora/corpus-10k --jobs 4

      # -S src (build files live there); -B stays repo-root-relative, and src/CMakeLists.txt
      # pins spike binary dirs to ${CMAKE_BINARY_DIR}/spikes/<x>, so the subject path below
      # (build/bench/spikes/parse-bench/...) is unchanged by the src/ move.
      - name: Configure (spikes ON, vcpkg toolchain)
        run: >
          cmake -B build/bench -S src
          -DCMAKE_BUILD_TYPE=Release
          -DCMAKE_TOOLCHAIN_FILE=$VCPKG_INSTALLATION_ROOT/scripts/buildsystems/vcpkg.cmake
          -DVCPKG_MANIFEST_FEATURES=spikes
          -DCONTEXT_BUILD_SPIKES=ON

      - name: Build parse-bench
        run: cmake --build build/bench --target context-parse-bench

      - name: Canonical fixpoint check (serialize∘parse == identity on canonical input)
        run: >
          ./build/bench/spikes/parse-bench/context-parse-bench canon-check
          --corpus bench/corpora/corpus-10k --sample 500

      - name: Run harness (median-of-5, R-QA-009 discipline)
        run: >
          python3 bench/harness.py
          --subject ./build/bench/spikes/parse-bench/context-parse-bench
          --corpus bench/corpora/corpus-10k
          --runs 5 --single-thread-attach --count 2000
          --out bench/results/ci-ubuntu-10k --label ci-ubuntu-10k

      - name: Upload bench results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bench-results
          path: bench/results/
          if-no-files-found: error

  license-gate:
    name: dependency-license gate (deny-by-default)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run license gate
        run: python3 tools/check_licenses.py

      - name: Upload SBOM
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sbom
          path: sbom.json
          if-no-files-found: error

  # Fast pytest gate over the repo's Python tooling (R-QA-013 retroactive coverage):
  # tools/tests (license gate) + bench/tests (seeded corpus generator).
  python-tests:
    name: python tests (license gate + corpus generator)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install pytest
        run: python3 -m pip install pytest

      - name: Run Python test suites
        run: python3 -m pytest tools/tests bench/tests -v
