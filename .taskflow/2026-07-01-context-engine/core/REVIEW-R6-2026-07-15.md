# Design Review R6 — 2026-07-15 — M8+ readiness (post-M7)

> **Trigger:** M7 (runtime UI) completed 2026-07-15 (engine `743f82e`, five blocking `m7-exit-*`
> gates green on the 3-OS matrix). This round reviewed the REMAINING stages — **M8 (build
> pipeline), M8.5 (wedge hardening), the §2 pre-release track, the §3 v2 boundary** — plus
> status currency, ahead of M8 task decomposition. Completed-milestone decisions were NOT
> re-litigated.
> **Method:** three parallel adversarial reviewers — **A** ground-truth vs the engine repo
> (`IvanMurzak/Context-Engine` @ `743f82e`), **B** external spec/standards conformance (current
> vendor/platform sources, fetched 2026-07-15), **C** internal consistency + completeness. All
> findings verified against doc text / repo code / cited sources before application; fixes
> applied in one batch 2026-07-15.

## Owner-flagged items (decisions this round could NOT make)

1. **Context Sim product disposition** — the §2 pre-release track never ran (single-lane
   execution; zero releases/tags). §2 rewritten as-designed/as-built; the R-FILE-011
   orchestration-density targets **re-anchored to "no later than the M8.5 exit"** (TD scheduling
   decision — the requirement is v1-MUST so the commitment cannot outlive v1). **Whether to ship
   "Context Sim" as a named product off the M8 build pipeline is an open owner decision** (§7).
2. **Production signing key mint + custody** — the R-SEC-009 verify-before-use gate exists with
   fail-closed tests, but the trust root is deliberately empty (engine `docs/signing.md`); the
   key mint + custody model is an owner decision gating M8's first release (§7).
3. **EU CRA applicability** (advisory) — reporting obligations start 2026-09-11; routed to
   business/legal counsel; CVD/readiness added as an M8.5 advisory item.

## Findings applied (consolidated across A/B/C; most severe first)

| # | Sev | Finding | Disposition |
|---|-----|---------|-------------|
| 1 | P0 | "Context Sim" track narrated as running/parallel; R-FILE-011 density targets anchored to the never-run pre-release (C1/A1) | §2 rewritten as-designed/as-built; targets re-anchored to ≤ M8.5 exit in R-FILE-011, ARCH §1.1, README gaps row, M8.5 exit; owner item added (§7) |
| 2 | P1 | Status headers in README/ARCHITECTURE/REQUIREMENTS said "M0–M6 / M7 next" (C2/A5) | All three updated to M0–M7 COMPLETE / M8 next (2026-07-15) |
| 3 | P1 | Windows code signing — v1-MUST per R-BUILD-005 but unnamed anywhere; M8 exit validated zero desktop signing (B F2/C3) | Windows Authenticode leg added to R-BUILD-005 + M8 bullet (Azure Artifact Signing, `context doctor` check, SmartScreen honesty); desktop-signing clause added to M8 exit; cert procurement named de-risk |
| 4 | P1 | "Neither Dawn nor wgpu-native has a vcpkg port" now false — official `dawn` port exists (~2026-06, incl. Tint tools) (B F1) | §5 risk row + ARCH §4 row corrected; noted the port makes Dawn L-42-conformant from-source |
| 5 | P1 | "Dawn re-evaluable at M4" expired with no recorded outcome; engine shipped M4–M7 on wgpu-native v29.0.1.1 SHA-pinned prebuilts (A3/C13) | As-built recorded in ROADMAP M4, ARCH §4, §5 risk row; re-evaluation re-homed to M8 (export templates freeze the shipped backend) |
| 6 | P1 | M0 signing claim overstated: verify gate + tests exist but production key unminted, trust root empty, fetch paths unwired (A2) | M0 text + exit annotated as-built; key-mint + fetch-verify wiring added to M8 de-risks + §7 owner item |
| 7 | P1 | Perf-isolated runner class never provisioned → all numeric perf gates advisory; "10k proxy until M3" expired silently (A4/C9) | §6 corrected (proxy remains per-PR; advisory-until-provisioned per R-QA-012); runner provisioning named M8 de-risk (else R-BUILD-006 budgets advisory-only) |
| 8 | P1 | Pack-format decision status unrecorded (M2 "co-designed" vs M8 "lands before") (C4/A7) | As-built recorded: format v0 co-designed at M2 (engine `docs/chunk-pack-format.md`); M8 freezes v1 + owns the doc's deferred list — in ROADMAP M8 + R-ASSET-005 |
| 9 | P1 | M8.5 exit/de-risks omitted the trailing-GUI bucket it homes (incl. R-HUX-006 MUST) (C5) | Trailing-GUI exit clause added (tilemap paint, in-context override edit, contextual help); de-risks extended |
| 10 | P1 | §8 "Immediate next steps" entirely M0-era, dangling task-file refs (C6) | Rewritten for the M8 era (adapters, signing, pack freeze, fetch-verify, doctor, runner provisioning, owner items, M8.5) |
| 11 | P1 | Android leg: Google developer-verification regime (2026-09-30 → global 2027, covers sideloads) + annual target-API ratchet unnamed (B F3) | Added to R-BUILD-001 Android paragraph + M8 signing bullet |
| 12 | P1 | EU CRA never named; reporting obligations 2026-09-11 (B F4) | Advisory M8.5 CVD/CRA-readiness bullet + §7 advisory item |
| 13 | P2 | M8.5 trust-tier bullet restated M1-shipped scope enforcement as new (C7) | Reworded as adversarial validation/hardening of the M1-shipped enforcement |
| 14 | P2 | v1 SHOULDs with no milestone home: R-PKG-006, R-QA-006 samples half, R-HUX-007/"v1.x" undefined, R-SIM-004, R-A11Y-002, R-NET-002 (C8) | "Unscheduled v1 SHOULDs — the v1.x ledger" subsection added after M8.5 |
| 15 | P2 | Stale "(naming is still open)" in ROADMAP M0 + L-57 (C10) | Both corrected (settled 2026-07-01) |
| 16 | P2 | §7/DD header framed the counsel EULA as a pre-push M0 gate; push happened 2026-07-13 with the v0.2.1 draft (C11) | §7 bullet + DD header + M0 exit annotated with the post-facto state; counsel review stays open |
| 17 | P2 | EULA draft banner said v0.2; version history + README say v0.2.1; effective-date placeholder resolvable (C12) | Banner bumped to v0.2.1; push date noted, effective date left for counsel |
| 18 | P2 | L-61 V8 supply-chain sub-item "owed M1–M3, NOT resolved" expired (C14) | As-built recorded: SHA-pinned prebuilt channel (`tools/fetch_v8.py`); root-signing rides the M8 key mint |
| 19 | P2 | ARCH §4 candidate rows resolved at M6 but unrecorded: Jolt-class REJECTED (custom Q16 fixed-point physics3d/2d); spatial package as-built (A6) | Rows annotated with as-built resolutions (engine `docs/physics-determinism-decision.md`) |
| 20 | P2 | emdawnwebgpu memory-growth constraint spike-dated, no upstream issue linked; JSPI not yet broad (B F5, verified F3-current) | §5 row: re-test at M8 template time + file/link upstream issue; Asyncify remains the M8 default |
| 21 | P2 | "Evergreen-browser-broad" WebGPU now true everywhere EXCEPT Linux browsers (B F6) | Linux-browser caveat added to the M8 web-adapter bullet |
| 22 | P2 | R-SEC-005 npm clause pre-dated 2025 registry hardening (trusted publishing GA, provenance default-on, classic tokens revoked) (B F7) | Clause freshened with the current mechanisms |
| 23 | P2 | "v1 = M1–M8.5" vs M0-inclusive spans elsewhere (C15) | Normalized to v1 = M0–M8.5 |
| 24 | P2 | R-OBS-006 priority letter missing the (v2) marker its §3 disposition assigns (C16) | Title now `SHOULD` (v2) |
| 25 | P2 | Month-based timeline residue vs actual cadence (M0–M7 in 13 days) (C17) | §4 as-built cadence note; §2/README occurrences fell with #1; §4 estimate kept as the original record |
| 26 | P2 | §6 blocking row listed R-BUILD-009 with no activation note (C9) | Annotated "activates at M8; samples-corpus smoke covered M1–M7" |

## Verified sound (the confidence half — spot highlights)

- **M7 as-built record is fully accurate**: all 12 task SHAs exist on main with matching
  subjects; five `m7-exit-*` ctests registered AND wired as a named blocking CI step on all
  3 OS legs (fleet-manifest rows present, manifest validated every run); CI green at `743f82e`.
- **M8's prerequisites exist as claimed**: the full R-QA-005 session surface (simTick, seed,
  hierarchical state hash, versioned replay kind with manifest-verify, `context determinism
  diff`); m6-exit determinism gates on the exact wedge matrix; the M3 contract freeze
  (protocolMajor=1 snapshot + additive-only CI-enforced catalog with `consent_required`
  reserved); the R-VER-004 `<root>/versions/<semver>/` layout as tested code; the R-SEC-009
  verify mechanism (16 fail-closed tests) and uniform SHA-pinned prebuilt fetch discipline.
- **`context doctor` + `build.*` catalog codes correctly do NOT exist yet** — they are M8 work,
  and the additive-only catalog makes adding them non-breaking.
- **External claims mostly current** (unusually so — the 2026-07-02 spike ratifications):
  notarytool/hardened-runtime/headless-CI path current, no 2026 Apple changes; `-sUSE_WEBGPU`
  removal + emdawnwebgpu-as-maintained-path exact; "Asyncify or JSPI when broadly available"
  precisely right mid-2026; WAMR zycore unpinned-fetch warning verified against upstream source;
  TUF/Sigstore still the correct v2 naming/architecture; L-57 royalty terms identical across all
  five documents; L-58 v1 staging consistent across every occurrence; trailing-GUI lists match
  across M5/M8.5/R-*; M1 contract staging mirrored exactly between ROADMAP and R-CLI-004.
- **Stale-term sweep clean** on the retired vocabulary: EditorCore/RuntimeCore only in the
  deliberate rename note; "subscription waiver" only as removal records; Apache-2.0 only for the
  MCP plugin family.

## Follow-ups (outside this folder)

- Engine repo's own `README.md` + `CLAUDE.md` still say "M6 complete / M7 next" — stale after
  `743f82e`; fix rides the next engine docs PR (M8 lane), not this design commit.
- At M8 template time: re-test the emdawnwebgpu memory-growth constraint and file/link the
  upstream issue (finding 20).
- `presentation.html` (2026-07-04 pitch-deck snapshot in this folder) still narrates the Context
  Sim fork as-designed — left unchanged as a dated historical artifact; regenerate it if it is
  ever re-presented.
