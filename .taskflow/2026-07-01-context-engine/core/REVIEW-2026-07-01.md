# Design Review — 2026-07-01 (consolidated findings & remediation tracker)

> Four parallel adversarial reviewers (consistency auditor **[A]**, feasibility skeptic **[B]**,
> market/scope veteran **[C]**, AI-workflow + security adversary **[D]**), ~110 findings, TD-verified.
> **Verdict:** the file-authoritative core architecture survived all four attacks — no reviewer
> found a reason to unwind L-19..L-31. The defects are: (1) load-bearing surfaces *assumed* by
> locked decisions but pinned by no requirement, (2) three genuinely undesigned hard problems,
> (3) missing scope statements with retrofit teeth, (4) heavy stale text from the same-day
> architecture evolution. All fixable on paper now; brutal to fix in M3/M5.

## Tier 1 — design-blocking (must be fixed before the milestone that inherits them)

| # | Finding | Fix → | Deadline |
|---|---|---|---|
| T1-1 | **Cross-language memory model undesigned** [B1]: zero-copy JS views (R-LANG-008) dangle when archetype migration moves memory; no view-lifetime/invalidation protocol; "undeclared access throws" (L-38) unimplementable on raw TypedArrays; GC-pinning of external ArrayBuffers constrains §2d | New R-LANG-009: views valid one system invocation; structural changes deferred to command buffers; detach-on-exit; debug-Proxy enforcement; M0 JS spike must prove cheap detach | M0/M3 |
| T1-2 | **One JS VM = one thread** [B13]: auto-parallel scheduler doesn't apply to the 90% TS tier; all TS systems serialize | Amend L-38/R-SIM-006 honestly: TS = single scheduler lane; parallelism = native/WASM/engine systems; isolate-pools post-v1 investigation | M1 |
| T1-3 | **Field-path rebase undefined over JSON arrays** [B5]: scene child lists are arrays; index paths under concurrent insert = mass conflict or silent wrong-entity write | Amend L-32/L-35: stable intra-file entity/component ids; child lists id-keyed; gesture paths = (entityId, component, path); structural-conflict rules | M2 schema |
| T1-4 | **npm install scripts bypass the sandbox** [D8]: postinstall = arbitrary code before the L-49 sandbox exists; agents install autonomously | New R-SEC-005: engine-driven installs always `--ignore-scripts`; script-requiring package ⇒ native-tier consent gate; lockfile integrity enforced | M0 |
| T1-5 | **Agent surfaces assumed, never pinned** [D1,D2,D3,D5,D6,D7]: schema introspection (incl. package-contributed component types), component-type authoring path, step-N/seed/input-injection, query capability (L-35 depends on it!), log capture topic, TS build diagnostics (+ L-32 "everything is JSON" contradicts TS source files) | New R-CLI-005 (schema introspection), R-LANG-010 (declarative component authoring), R-QA-005 (step/seed/inject/replay/state-hash), R-CLI-006 (query surface + provenance), `log` topic + `context logs`, R-FILE-003 extension to TS diagnostics, L-32 code-file carve-out | M1–M3 |
| T1-6 | **Editor extensibility contract absent** [C2]: no requirement lets packages contribute inspectors/gizmos/panels — ecosystem make-or-break, Unity-decade retrofit | New R-EDIT-001 (MUST): versioned, sandbox-respecting editor UI extension contract, designed in M5 | M5 design |
| T1-7 | **Stale architecture text** [A1,A10,A13]: README flagship paragraph + plan §2.1 diagram still describe command-bus + TS-via-WASM (both superseded); L-8 row never reconciled with L-40 | Rewrite README paragraph + stack line; redraw §2.1 (files layer, watch/derive arrows, presentation-modules box); annotate L-8/L-1 | now |

## Tier 2 — majors (fix in the remediation pass)

- **Format/hashing:** `.gitattributes` requirement + CRLF/BOM diagnostics (verified real hazard) + canonical float spec + NFC [B2]; two-hash definition (raw-byte for watch/CAS, canonical for derivation keys) + resolve the never-reformat clause: tool saves canonicalize what they write [B14].
- **Daemon write topology:** expected-writes table (self-echo); enumerate the ONLY daemon-initiated authored writes (meta creation, GUID move-heal, `context validate --fix`, hint-heal), each idempotent — resolves R-FILE-003 vs L-34/L-36 contradiction [B3].
- **Scale-envelope realism:** persisted reconcile index (gitignored, rebuildable); restate R-FILE-011a (index-warm ≤5s; fresh-worktree first attach = throughput-bounded with progress); scope template-fan-out + validation as async-streamed in 011b; per-stage latency budget table = M1 exit criterion [B4]. Fine-grained cache keys (derived-format version per artifact kind, NOT blanket engine version) so engine patches don't cold-start every project; upgrade-cost target added [B11].
- **Multi-writer:** define dependency-safe order normatively (GUID-first move; hints heal lazily; drop rename's rewrite-all-referencing-scenes, or make it explicit `--heal-refs`); daemon verbs CAS every write [B10]. Write fairness/rate bounds per client [D13].
- **Rendering:** CEF fallback decision tree in L-41 (windowed-child-viewport / software-OSR-chrome / inverse composition) + risk-table row [B6]. WebGL2 T0 → **owner ruling Q2** [B7]. Baked-lighting format hooks (UV2 reservation, material/pack lightmap inputs) [C16].
- **TS tier:** interpreter perf floor + per-platform throughput matrix + M3 interpreter-mode exit criterion + profiler attribution for promote-to-WASM decisions; §2d weighs interpreter throughput + debugger availability [B8, C8]. R-OBS-005: source-map-mapped debugging end-to-end (CDP/DAP), TS-resolved stack traces everywhere incl. headless [C8].
- **Determinism:** importer run-determinism (fixed seeds, thread-independent reductions) + CI double-run byte-compare; explicit cross-machine decision (ISA in cache key vs strict-FP path) [B9]. Physics determinism config requirement (Jolt cross-platform-deterministic mode) wired into L-54's CI gate [C11].
- **Security:** importer isolation tier (WASM/subprocess) + fuzzing [D9]; per-OS endpoint ACLs (Windows named-pipe DACL, temp-file ACLs) [D10]; MCP scopes + R-ARCH-001 "equal capability surface, authorization may restrict" amendment [D11]; project-root path jail incl. symlink/junction escapes [D12]; cache self-verification defined + per-user ACL isolation [D14]; package provenance SHOULD [D15].
- **Ecosystem:** R-PKG-005 engine-compat field (`engines`-style) required from day one [C14]; R-VER-004 side-by-side engine versions + launcher resolution [C10]; R-OBS-006 editor crash reporting (opt-in minidumps) + update channel [C10]; R-FILE-012 structural merge driver + post-merge validate gate SHOULD [C3]; R-ASSET-005 chunked pack format + runtime GUID load API (before M8 freezes the format) [C7]; animation-graph asset kind SHOULD + timeline WON'T(v1) [C5]; audio authored kinds line [C12]; templates/samples COULD [C9].
- **MoSCoW repairs** [A22–A25]: R-BRIDGE-005, R-COLLAB-001/002 → MUST (architecture already MUST-level); R-VER-003 engine-pin half → MUST (arbiter of R-BRIDGE-006); R-CLI-003 → MUST (R-BUILD-002 depends); R-KERNEL-006 annotated per L-43 (dynamic load ≠ hot-swap).
- **RPC budgets:** session-query p99 target, paged/cursor query contract, inspector push-subscription; derivation events carry input content-hash for write-acknowledgement [B12, B16]. Lock file inside project (byte-range) not path-derived [B15].
- **Structural provenance events** and remaining [D] minors folded into the same sections.

## Tier 3 — mechanical staleness (apply wholesale, no judgment needed)

All remaining [A] findings: R-ARCH-001/R-QA-004/R-CLI-004 rewording; §3.3 clients; tech-stack rows (WASM scope, vcpkg-only, candidates-not-locked); Conan removals (M0, §3.4); O-1→R-DATA-005 pointer; §2a header L-range; closure-paragraph placement + Part A table formatting + provenance column; L-10 annotation; Part A′/Part B headings + dependency-notes staleness; terminology entries (event bus vs event stream; authored code files); "TS/WASM tier"→"TS tier"; M2/M5/M10 milestone rewording per locks; §7 risk-table C-ABI mitigation; §8 dedupe; §9 next-steps; README how-to-review/order/doc-table; `context validate --fix` prefix; R-ARCH-005 in-process mode; R-COLLAB-002 cross-ref; M0 "four→five" in §9; de-risk list assignments [A38].

## Owner rulings (asked 2026-07-01 — RULED 2026-07-01, all final)

1. **2D position** [C1]: recommend WON'T(v1) + binding non-preclusion clauses on M4 renderer/M5 viewport/transform component.
   → **RULED: recommendation overridden — 2D is first-class in v1 (L-55):** sprite path in M4, Box2D-class 2D physics package in M6, tilemap asset kind + 2D viewport mode in M2/M5; transform not 3D-only (R-2D-001…003).
2. **WebGL2 T0 tier** [B7]: recommend demoting to post-v1 (WebGPU is evergreen-broad; T0 doubles M4) — alternative: keep + publish T0 capability contract.
   → **RULED: demoted (L-56):** v1 web builds require WebGPU; tiers collapse to T1 (WebGPU baseline) / T2 (native advanced); WebGL2 may return post-v1 as an optional constrained package.
3. **Business model / engine license** [C4 + TD analysis]: Epic-style proposed (source-available EULA · 5% > $1M lifetime · ai-game.dev-subscription waiver · plugins stay Apache-2.0); regardless of choice: CLA/DCO + dependency-license allowlist in CI from first commit (O-7).
   → **RULED (L-57):** source-available under a proprietary EULA (NOT Apache-2.0); **2% royalty** on gross revenue per game above $1M USD lifetime (deliberately under Epic's 5%); terms locked per engine version (never retroactive); royalty waived for games developed under an active ai-game.dev subscription; MCP plugin family stays Apache-2.0; CLA/DCO + dependency-license allowlist in CI from the first commit; EULA legal drafting deferred to counsel (business-dept/legal task).
4. **Remediation delegation**: apply Tiers 1–3 + O-2 split (string-table asset kind in M2; shaping/IME as L-53 capability rows; editor-l10n post-v1) + O-3 (editor ARIA/keyboard discipline from M5; runtime a11y-primitives package SHOULD).
   → **RULED: approved as specified** — applied in this remediation pass (R-I18N-001; R-UI-005 amendment; R-A11Y-001 MUST / R-A11Y-002 SHOULD; editor-l10n post-v1 noted in §24).

## Status

- [x] Remediation pass applied (docs amended, new R-*/L-* entries, staleness fixes) — 2026-07-01. Added: L-55…L-57; R-CLI-005/006, R-LANG-009/010/011, R-QA-005/006, R-SEC-005…008, R-EDIT-001, R-FILE-012, R-ASSET-005, R-PKG-005, R-VER-004, R-OBS-005/006, R-SYS-008, R-REND-006, R-2D-001…003, R-I18N-001, R-A11Y-001/002 + the amendments marked inline across all four docs.
- [x] Owner rulings recorded (Q1–Q4) — see the ruled section above; L-55/L-56/L-57 in DESIGN-DECISIONS.md.
- [x] M0 task files updated (spike criteria: detach cost, interpreter throughput, CEF fallback branch; bootstrap license line + CLA/DCO + license-allowlist DoD).
- Remediation PR: **#110** (`worktree-ctx-remediation` → `main`).
- Full reviewer outputs: session transcript 2026-07-01 (agents: consistency, feasibility, market, ai-workflow+security).
